1. Обойти диск, начать с какой-то точки монтирования и найти все файлы по маске.
    fing(".", "*.jpg) -> ["...","...",...]
    консольный ввод
2. Папка для мини-модулей. Первый модуль - logger. Читать про escape codes
    logger.message() - печатает + время
    logger.info() - зелёным
          .warning() - жёлтым
          .error() - красным
          .fatal() - красный + выход
    создаэтся log-файл, где всё это сохраняется, каждое сообщение в начале имеет время и тип
3.